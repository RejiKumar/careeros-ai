# Assets

Brand assets (app icon, splash screen, adaptive Android icons, monochrome icon) are required before a store build. They will be added during Milestone 5 readiness alongside the closed-test checklist.

Until then, `app.config.ts` intentionally references no icon or splash image files.
