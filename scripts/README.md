# Scripts

Repository utilities live here. Expected entries:

- `generate-contracts` — generate typed API contracts from the FastAPI OpenAPI document into `packages/api-contract` (Milestone 1).
- `check-secrets` — scan commits/diffs for accidental secret or private-data commits (before merge).
